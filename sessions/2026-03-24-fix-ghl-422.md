# Session Summary

**Date:** 2026-03-24
**Focus:** Fix 422 error on CRM page manual GHL refresh

## What Got Done
- Diagnosed 422 error on `/opportunities/search` endpoint — snake_case params (`location_id`, `pipeline_id`) rejected by GHL API v2
- Fixed `getOpportunities()` to use camelCase params (`locationId`, `pipelineId`) matching all other GHL endpoints
- Improved error handling across all GHL API functions to capture response body on failures
- Committed and pushed fix to `main`
- Established session logging process: `sessions/` folder with individual log files per session
- Created `DECISIONS.md` at project root
- Updated `CLAUDE.md` Session Log Protocol to use `sessions/TEMPLATE.md` format and made it mandatory

## Decisions Made
- GHL API params must be camelCase (see DECISIONS.md)
- Session logs stored as individual files in `sessions/` instead of one monolithic file
- Session logging is now mandatory at end of every Claude Code session (planning or execution)

## Open Items / Next Steps
- Verify CRM refresh works after Vercel redeploy
- If other GHL endpoints surface similar issues, audit all param formats

## Files Changed
- `src/lib/ghl.ts` — fixed snake_case → camelCase params, added response body to error messages
- `CLAUDE.md` — updated Session Log Protocol and Decision Log Protocol sections
- `DECISIONS.md` — created with initial entries
- `sessions/2026-03-24-fix-ghl-422.md` — this file

## Memory Updates
- Preferences learned: Bobby wants session logs written every session, no exceptions — for context handoff between windows
- Decisions to log: see DECISIONS.md entries for 2026-03-24
